import { ImgboxDirective } from './imgbox.directive';

describe('ImgboxDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgboxDirective();
    expect(directive).toBeTruthy();
  });
});
